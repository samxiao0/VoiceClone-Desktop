# 🎤 VoiceClone Desktop App

A fully offline GPT-TTS powered voice cloning desktop app built with **Python + Gradio + PyWebView**.

### Features
- Offline speech generation
- Choose performance mode: Lite / Moderate / High
- Save generated WAVs locally
- Clean native desktop window (no browser)

### Run
```bash
pip install -r requirements.txt
python voiceclone_app.py
